const description = 'Finds files that exceed the configured size limit and may bloat your repository.';

function check() {
  // TODO: Implement large file detection
  return [];
}

module.exports = { check, description };

